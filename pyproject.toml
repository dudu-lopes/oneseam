[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py313"
exclude = [
  ".git",
  ".venv",
  ".tmp",
  ".tmp_tests",
  ".oneseam_local",
]

